# Use CUDA-enabled PyTorch wheels (CUDA 11.8)
# Keep PyPI as the primary index and add the PyTorch wheel index as an extra
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
numpy>=1.24.0
# Pin matplotlib to a PyPI release compatible with numpy>=1.24
matplotlib==3.10.7
seaborn>=0.12.0
tqdm==4.66.5